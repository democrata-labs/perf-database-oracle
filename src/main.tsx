import React from 'react'
import ReactDOM from 'react-dom/client'
import OraclePresentation from '../oracle_perf_pres.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <OraclePresentation />
  </React.StrictMode>,
)

